export interface DeleteMemberProps {
  text: string;
  lineBreaking: string;
  subText: string;
  leftButtonText: string;
  rightButtonText: string;
  leftHandler?: () => void;
  rightHandler?: () => void;
}
